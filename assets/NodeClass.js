export default class NodeClass {
  constructor (index, value, width, height, color) {
    this.index = index
    this.value = value
    this.width = width
    this.height = height
    this.color = color
  }
}
